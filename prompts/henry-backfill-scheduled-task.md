# Scheduled task: repair one historical Matthew Henry fallback

Work only in the Daily Bible Reader project in an isolated worktree. This independent historical-maintenance lane never prepares a daily study, never runs `study:next`, and never blocks or waits for the current-through-T+7 daily lane. It may process at most one already manifest-backed chapter per run, replacing a verified full-commentary fallback only after a reviewed verse layer is safely published.

Before acting, read `AGENTS.md`, `README.md`, `PROJECT_STATE.md`, `docs/AUTOMATION_RUNBOOK.md`, `docs/CONTENT_AUTOMATION.md`, `docs/COMMENTARY_WORKFLOW.md`, `docs/MATTHEW_HENRY_PIPELINE.md`, `docs/SECURITY.md`, and `docs/RELEASE_STABILITY.md`. Resolve the canonical checkout from the absolute Git common directory. The scheduler may start in a dirty shared checkout: do not modify, switch, reset, clean, or discard it. Fetch `origin/main`, verify it, create a temporary task worktree, and link only the existing ignored `private-content`, `private-commentary`, `research/raw`, and `research/working` directories after confirming each target is a real directory inside the canonical checkout. Never create replacement empty private storage.

1. Run `npm run mhc:backfill:next` and validate its JSON against `schemas/mhc-backfill-work-order.schema.json`. Its cooldown-aware least-recent-attempt queue selects at most one manifest-backed verified fallback. `none` with `published_henry_fallbacks_cooling_down` means report the safe retry time and do not bypass the cooldown.
2. For `generate_review_publish`, write only the embedded one-reading request to ignored private staging, validate it, and run `npm run mhc:ensure -- --request <that-file>` without `--max-retries`. It permits one actual Codex child process for exact `gpt-5.3-codex-spark`; only an eligible Spark model-execution/no-output failure may run one exact `gpt-5.6-luna` child at low reasoning. Never use Sol, Terra, or any other model. Deterministic request, source, checksum, schema, security, repository, review, validation, or publication failure never switches models.
3. For `review_attach_publish` or `attach_publish`, follow the existing hash-bound review and checksum catalog procedures. Process no second reading. Do not rewrite the orientation, main synthesis, takeaway, sources, ESV reference, comments, or highlights.
4. On a successful reviewed attachment, revalidate, upload versioned metadata first, promote the existing private manifest last, and verify exact Drive bytes and unchanged restricted sharing. On every failure, retain the verified source link and the prior manifest; do not publish a partial layer.
5. Immediately after every selected attempt, record only normalized safe state with `npm run mhc:backfill:record -- --reading <readingId> --outcome <published|model_failure|blocked> --stage <generation|review|validation|publication|controller> --code <SAFE_CODE>`. Use only the controller's normalized all-caps code; never copy raw error text into state or output.
6. Report one concise safe result in this task's own output: `lane=henry_backfill`, reading ID/date when known, action, normalized stage/code, whether the prior manifest remains live, queue/cooldown status, and exact retry or diagnostic action. Never include private prose, source atoms, credentials, URLs with secrets, reader identities, comments, ESV wording, or Google resource IDs.

Never create Google resources, change sharing, deploy, push, or commit without separate authorization. A selected failure is a completed safe attempt: record it, preserve the fallback, and let the next independent run rotate to the least-recent eligible debt item.
