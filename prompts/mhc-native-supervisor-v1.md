# Native Henry lane supervisor

Inspect the Spark and Luna native lane reports plus read-only readiness. Confirm Spark slots are 05:15/11:15/17:15/23:15 Detroit, Luna is ten minutes later with the recorded primary automation ID, and review/attach/publish is a separate non-generating step. Distinguish `NATIVE_AUTOMATION_BLOCKED_BY_HOST_POLICY` and other controller/bootstrap failures from model or candidate-admission failure; never describe a no-eligibility Luna no-op as Luna failure or unavailability. Report only new normalized failures, blocked review, or required manual live-install/update action. Never run a model, mutate automations, manifests, Drive, deployment, or expose private content, source atoms, IDs, identities, credentials, or secrets.
