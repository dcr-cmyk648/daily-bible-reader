# Native Henry Spark lane

Run at 05:15, 11:15, 17:15, and 23:15 America/Detroit using exact `gpt-5.3-codex-spark` at medium reasoning. Fetch `origin/main`, create an isolated worktree, and verify/link the existing ignored private stores; never substitute empty stores. Run exactly `npm run mhc:native:prepare -- --model gpt-5.3-codex-spark --automation-id <this-automation-id>`. Prepare durably binds this Spark/Luna slot to one reading and exact source/chunk state before it creates a model work item. If it reports `state=deterministic_block`, stop successfully with that safe code; the paired Luna run must consume the same block and may not select another reading. If its safe JSON report includes `workItemPath` for a prepared or already-active lease, use that repository-relative path exactly. Read only that directory's bounded `source-view.json`, `instructions.txt`, copied schemas, and `prompts/mhc-native-spark-worker-v1.md`; actually generate `candidate.json` before the first submit. A candidate is incomplete—not generated—if it has zero fact briefs, zero verse drafts, or lacks either record for any requested verse. Run `npm run mhc:native:submit -- --work-item <workItemPath>`. After every failed submit, you MUST read only that work item's `validation.json`, repair `candidate.json` with materially different bytes, and resubmit against the same lease. Make at most two repairs (three submit attempts total), and use both remaining repairs after the first failure unless an earlier repair validates. The controller rejects unchanged candidate bytes and refuses early terminal failure; unresolved terminal failure requires all three distinct failed submits. Only then may Spark run `npm run mhc:native:fail -- --work-item <workItemPath> --code NATIVE_CANDIDATE_UNRESOLVED`; do not record historical cooldown, because the terminal Spark failure is eligible for Luna. Never report success after a failed submit. Run `npm run mhc:native:assemble -- --work-item <workItemPath>` only after a successful submit. `action=none/state=reading_incomplete` is normal multi-chunk progress: exit successfully and wait for later chunks; it never creates a review handoff. Handle one chunk/reading boundary only. Do not review, apply, sync, modify a manifest, publish, push, or expose private prose, source atoms, IDs, or secrets. Safe-report normalized lane/state/code and that fallback/manifest remain unchanged on failure.
