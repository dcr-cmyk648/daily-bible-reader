# Scheduled task: keep the Daily Bible Reader ready through T+7

Work only in the Daily Bible Reader project in an isolated worktree. This is offline prepublication work; the deployed reader must never call a model. The user has authorized this exact recurring lane to keep every scheduled reading from the current Detroit day through seven civil days ahead prepared. Each validated work order still authorizes only one reading and one atomic publication. A caught-up run therefore prepares only the newly entering T+7 study; a recovery run may reevaluate after each successful exact Drive readback and repair the earliest remaining gap until the bounded eight-reading horizon is complete. This is not authorization for a reading beyond T+7, the new long-term plan, comments, ESV text storage, or unrelated external changes.

Before acting, read `AGENTS.md`, `README.md`, `PROJECT_STATE.md`, `docs/AUTOMATION_RUNBOOK.md`, `docs/CONTENT_AUTOMATION.md`, `docs/COMMENTARY_WORKFLOW.md`, `docs/EDITORIAL_STANCE.md`, `docs/CONTENT_AND_RIGHTS.md`, `docs/MATTHEW_HENRY_PIPELINE.md`, `docs/SECURITY.md`, and `docs/RELEASE_STABILITY.md`. Read `.agents/skills/draft-daily-commentary/SKILL.md` completely. Resolve the canonical checkout as the parent of the absolute `git rev-parse --git-common-dir` result. The scheduler may start in a dirty shared checkout; do not modify, switch, reset, clean, or discard anything there. Fetch `origin/main`, verify that ref, and create a temporary task worktree from the fetched `origin/main` before making changes. If the fetch or isolated worktree cannot be created, stop and report the same T+7 gap. The canonical checkout's ignored stores are `private-content`, `private-commentary`, `research/raw`, and `research/working`. In the temporary worktree, create only ignored symlinks back to those exact existing directories when they are absent; first verify every target is a real directory and remains inside that canonical checkout. Never create a replacement empty private store. If the temporary task worktree is dirty with unrelated work or canonical private storage is unavailable, stop and report the same T+7 gap.

1. Run `npm run study:next` and validate the JSON against `schemas/rolling-study-work-order.schema.json`. The evaluator scans the complete current-through-T+7 horizon and returns its earliest missing or stale record.
2. If the action is `none`, verify the complete horizon's exact local hashes, reviewed Henry layers or valid fallbacks, manifest membership, and Drive readback, then run `npm run study:live-health`. It is the final authenticated app-visible readiness gate and must exit successfully before recording the primary lane as complete. If `plan_complete`, run the same command when the active horizon is nonempty, then continue to that same check.
3. For `prepare_publish`, work on only the returned `readingId`. The complete active calendar is already compiled into the current backend; do not invent calendar entries. `planExtensionRequired` refers only to the separate rollback-compatible private prepared-prefix plan. If it is true, run `npm run prefix:extend -- --today <YYYY-MM-DD>` immediately before manifest promotion only when this is the exact next contiguous active-calendar prepared-prefix reading. If an earlier private-prefix reading is missing, stop and report that gap rather than skipping it.
4. Explicitly invoke `$draft-daily-commentary` with the unchanged rolling work order. The primary task—not Spark—must perform broad source discovery, lawful consultation, source-status decisions, editorial weighting, drafting, editing, citation verification, coverage assessment, and final review. Use content-rich, readable, practical prose for expert Christian readers under the documented confessional stance. Do not claim consultation from snippets or inaccessible works.
5. Use the Spark-first controller only for the complete verse-by-verse Matthew Henry layer for this reading. It tries exact `gpt-5.3-codex-spark` first; one Spark model-execution failure (quota, availability, or process failure) may retry exact `gpt-5.6-luna` once with low reasoning. Sol, Terra, and every other model are forbidden. Deterministic request, source, checksum, schema, security, repository, review, and publication failures never fall back. Generate the one schedule reading, prepare its hash-bound review candidate, and compare every condensation with every cited exact atom. Fix a recurring error in the general controller/prompt and regenerate. Record any genuinely isolated correction and its reason in the same candidate. Only after the full comparison, set that candidate to `approved` and apply it once. Never apply an `in_review` candidate and then replace its correction history with a second approval record; the canonical approved record must stay bound to the raw generation so rebuilds can replay it. Attach the newest reviewed artifact from the checksum-bound Henry library. If both model attempts fail, retain or attach a verified HTTPS `henrySourceLink` to the complete public-domain chapter commentary, bind it to a real source-registry ID, and continue the independently researched study without suppressing historical context or other non-Henry components.
6. Finish every required component: plan entry, orientation, Scripture reference(s) without ESV wording, verse-of-the-day reference, coherent main synthesis, concrete takeaway, custom cited deep sections, source registry/coverage, either a reviewed Henry layer or the verified full-commentary fallback link, metadata, and exact content hash. Record `generation.contentProtocolVersion: daily-study-protocol/v1` and exactly one `componentAssessments.historicalContext` result. When meaningful reading-specific archaeological or historical evidence exists, set it to `included` and include both exact H3 layers: `### Archaeological and historical context` as the concise preview and `### Archaeological and historical context — expanded study` as a separately researched dossier. Omit both when they would be filler only when the assessment is `not_material` with a concise rationale. Once the preview is included for a prepared study, the expanded study is required: it must use distinct, materially fuller prose, at least two passage-specific H4 headings, explicit evidence-versus-inference boundaries, inline claim citations, and a nearby bibliography; never repeat or mechanically stretch the preview. The takeaway should default to one achievable inward focus for that day—an attitude, temptation, motive, habit of attention, or response to watch—and use outward action only when specific, realistic, and textually warranted; never assign vague work such as “fix an injustice.” Mark it `in_review` only after direct primary review.
7. Run source, private-content, schema, citation, rights, repository-safety, test, build, and release checks. Do not weaken a failed check.
8. Publish atomically to the existing private Drive structure: upload/version content, metadata, plan, config, and registry first; update the private manifest last; verify exact-byte readback, manifest bindings, and unchanged narrow sharing. Never include IDs or private content in the report.
9. If code or factual plan metadata changed, commit and push only safe tracked files, publish the code-only Pages release, and update only the existing approved token Apps Script path if necessary. Never move the immutable USER_ACCESSING production deployment pointer.
10. After exact Drive readback verifies the named reading, run `npm run study:live-health`, then rerun `npm run study:next` and validate the fresh work order. The live-health command combines the tracked public endpoint with Dustin's bearer credential from existing ignored `private-content/reader-codes.json`, accepts no token argument, and emits only safe status/count/date/reading-ID/component data. Its `currentHorizonHenryLayer` status covers chapter entries in the same fetched Detroit current-through-T+7 batch only; it does not expand the live fetch to historical fallback debt. Any nonzero result stops the run. If the work order returns another `prepare_publish`, repeat steps 3–10 for only that newly named earliest gap. This recovery loop is bounded by the eight-reading horizon and must stop immediately on the first generation, review, validation, upload, sharing, readback, or live-health failure. If it returns `none` or `plan_complete`, the primary preparation lane is complete.
11. Only after the complete current-through-T+7 horizon is end-to-end ready and its Drive readback is verified (or the reference plan is complete), run `npm run study:protocol-backfill:next` and validate it against `schemas/protocol-backfill-work-order.schema.json`. This optional historical lane may select at most one already-read manifest-backed stale study in deterministic most-recent-first order. A `refresh_review_publish` order is a genuine editorial pass: preserve its stable reading ID, comments/highlights, newest reviewed Henry layer or verified fallback, source provenance, and prior versions; update content first and the manifest last only after direct primary review and exact Drive readback. It never stores ESV wording or changes backend authorization. Historical Henry fallback debt belongs only to the independent Henry-backfill task and must not be inspected or processed here.
12. Report the daily-study ready-through state and the `currentHorizonHenryLayer` complete/debt count for the current-through-T+7 chapter batch, then every repaired reading/date in order, any protocol-refresh action, gates, Drive readback, ready-through date, commit/release, and any retry needed. On any failure, emit one concise safe diagnostic in this task's own output: `lane=daily_t_plus_7`, reading ID/date when known, a normalized stage/code, whether the prior manifest remains live, and the exact retry or diagnostic action. Never include private prose, source atoms, credentials, URLs with secrets, reader identities, or Google resource IDs.

Never generate a second reading under one work order or more than one protocol refresh; skip a gap; move beyond T+7; store or request ESV text; read comments; expose credentials or identities; create Google resources; alter sharing; or place private content/source atoms in Git. A failed reading leaves the prior private manifest current and retries that same gap next time.
