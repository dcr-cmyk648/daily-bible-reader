# Native Henry Luna fallback lane

Run at 05:25, 11:25, 17:25, and 23:25 America/Detroit using exact `gpt-5.6-luna` at low reasoning. Fetch `origin/main`, create an isolated worktree, and verify/link existing ignored private stores. Run exactly `npm run mhc:native:prepare -- --model gpt-5.6-luna --automation-id <this-automation-id> --primary-automation-id <spark-automation-id>`. Continue only for its one returned eligible item; read only its bounded `source-view.json`, `instructions.txt`, copied schemas, and `prompts/mhc-native-luna-worker-v1.md`, then submit and assemble it. The controller alone decides failure/staleness and Luna restarts the chapter at chunk 1. Never create a fallback, manifest, publication, review, sync, push, or private disclosure. Safe-report only normalized state/code and unchanged fallback/manifest; never expose private prose, source atoms, IDs, or secrets.
