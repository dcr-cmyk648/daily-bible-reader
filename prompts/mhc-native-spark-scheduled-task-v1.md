# Native Henry Spark lane

Run at 05:15, 11:15, 17:15, and 23:15 America/Detroit using exact `gpt-5.3-codex-spark` at medium reasoning. Fetch `origin/main`, create an isolated worktree, and verify/link the existing ignored private stores; never substitute empty stores. Run exactly `npm run mhc:native:prepare -- --model gpt-5.3-codex-spark --automation-id <this-automation-id>`. If its safe JSON report includes `workItemPath` for a prepared or already-active lease, use that repository-relative path exactly. Read only that directory's bounded `source-view.json`, `instructions.txt`, copied schemas, and `prompts/mhc-native-spark-worker-v1.md`; actually generate `candidate.json` before the first submit. Run `npm run mhc:native:submit -- --work-item <workItemPath>`. If submit fails, read only that work item's `validation.json`, repair `candidate.json`, and resubmit against the same lease. Make at most two repairs (three submit attempts total). If Spark still cannot validate, leave its lease and fallback/manifest unchanged; do not record historical cooldown, because the eligible stale Spark lease remains available to Luna. Run `npm run mhc:native:assemble -- --work-item <workItemPath>` only after a successful submit. Handle one chunk/reading boundary only. Do not review, apply, sync, modify a manifest, publish, push, or expose private prose, source atoms, IDs, or secrets. Safe-report normalized lane/state/code and that fallback/manifest remain unchanged on failure.
