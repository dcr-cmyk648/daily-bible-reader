# Prepare D078 / Zechariah 8 through T+7

## Goal

Prepare, directly review, validate, and privately publish exactly `CC-Y3Q4-D078` (Zechariah 8) for 2026-09-01 under rolling work order `RSWO-529105e440790acd97093fa5`, then inspect at most one Henry-only backfill.

## Requirements

- Keep the deployed reader free of runtime AI and store no ESV wording.
- Work only in this isolated worktree and the four verified canonical ignored stores.
- The primary task owns source consultation, editorial weighting, drafting, citations, coverage, review, validation, and publication.
- Spark may produce only the complete 23-verse Matthew Henry layer. The saved automation instruction forbids a Luna or other model substitution on Spark quota/unavailability; use a verified complete public-domain chapter link instead.
- Upload versioned private payloads first, promote the existing private manifest last, then verify exact bytes and unchanged sharing.
- Do not read comments, generate another study, move beyond T+7, create Google resources, or alter sharing.

## Relevant state and decisions

- Fetched `origin/main` is `b92be85e6b9f5b2731b038f331f9b52c226c656f`.
- The shared checkout is dirty and remains untouched.
- The private prepared prefix and reviewed Henry library are live through D077.
- `bridge:extend` appended only source day 78 to the rollback-compatible private prefix. The fresh internally schema-validated work order names the same D078 reading for 2026-09-01 with `planExtensionRequired: false`.
- The stricter saved automation instruction controls the model fallback conflict in current tracked documentation.

## Milestones and acceptance

1. Extend the rollback-compatible private prefix only through source day 78, rerun the work order, and confirm the same reading now matches the active plan.
2. Invoke the project commentary skill, consult lawful sources, write the complete staging package, and directly review all claims, prose, citations, and coverage.
3. Generate the one Spark Henry candidate or record a coded Spark availability failure and use the verified full-source fallback; compare every generated condensation with every cited atom before approval.
4. Pass source, schema, citation, rights, private-content, safety, test, build, release, and bundle gates.
5. Publish data-first/manifest-last, verify exact Drive bytes and ACL shape, and confirm `study:next` reports the horizon ready through 2026-09-01.
6. Run the one-reading backfill evaluator and perform no more than its one authorized action.
7. Commit and push only safe tracked factual plan/state changes; publish code-only Pages artifacts only when required by those changes.

## Validation

- Narrow validators for the work order, source registry, commentary metadata/hash/citations, rights, and Henry review candidate.
- `npm run validate:sources`, `npm run validate:private`, `npm run safety`, `npm run build`, `npm run publish:pages`, and `npm run check` as applicable.
- Private bundle validation followed by exact-byte Drive readback, manifest binding verification, and unchanged narrow sharing.

## Progress

- [x] Read automation memory, project instructions, workflow documents, skill, schemas, and fetched-revision state.
- [x] Fetched and verified `origin/main`; created a clean isolated worktree; verified and linked the four canonical ignored stores.
- [x] Validated the rolling work order selecting only D078.
- [x] Extend the private prefix through D078 and revalidate the same work order target.
- [x] Research, draft, and directly review D078.
- [x] Record the Spark quota failure and verify the complete Matthew Henry fallback.
- [x] Validate and publish atomically.
- [x] Reevaluate T+7, skip the prohibited same-run backfill probe, and update project state.
- [x] Finalize the safe tracked release state.

## Discoveries

- The first work order required the expected contiguous prefix extension. After extending only source day 78, the same reading remained the sole T+7 gap and the plan-extension flag cleared.
- Exact Spark returned an account-quota failure before generation. The saved automation forbids substitution, so no condensed layer exists and the separate Henry-backfill probe must be skipped.
- The CCEL Zechariah 8 page was verified from its Chapter VIII heading through commentary on every verse range and the Chapter IX boundary, qualifying it as the registered full-chapter public-domain fallback.
- All five versioned payloads and the manifest matched local bytes exactly after Drive readback. Payloads remain owner-only, while the in-place manifest retains only owner and reader permissions and its established parent.
- The post-publication work order is schema-valid with `action: none` and `reasonCode: horizon_ready` through 2026-09-01. The backfill evaluator was not invoked because this run had already received the Spark quota failure.
- Repository safety, all private source/citation/rights gates, 232 tests, every build, Pages publication/verification, and the 53-file private bundle passed. Frontend and PWA release bytes remain unchanged.

## Exact next action

On the next Detroit daily boundary, reevaluate the rolling work order; the expected next T+7 target is D079/Zechariah 9 for 2026-09-02.
