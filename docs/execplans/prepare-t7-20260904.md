# Prepare Daily Bible Reader through T+7 — 2026-09-04

## Goal

Prepare and atomically publish the earliest missing or stale reading in the Detroit current-through-T+7 horizon, then reevaluate after exact Drive readback until the bounded horizon is complete. Run one protocol refresh only after the primary lane is verified complete.

## Requirements and constraints

- Work only from the fetched `origin/main` isolated worktree; leave the dirty canonical checkout untouched.
- Process one validated rolling work order and one atomic manifest-last publication at a time.
- Keep Scripture wording, private IDs, credentials, comments, and raw copyrighted source text out of Git and Pages.
- The primary task owns source discovery, drafting, editing, citation review, and publication decisions.
- Exact Spark, with one Luna-low model-execution retry, is the only permitted Henry generation route; a verified complete public-domain link is the fail-closed fallback.
- Extend the private prepared-prefix plan only for the exact next contiguous reading, immediately before manifest promotion.
- Require exact payload readback, exact rolling-plan/manifest-prefix equality, unchanged narrow sharing, and authenticated bootstrap health before reevaluation.

## Repository state

- Base: `origin/main` at `df94a00e6c8093c812668fce134c18d890d18665`.
- Existing prepared prefix is now `CC-Y3Q4-D054` through `CC-Y3Q4-D088`, covering the complete Detroit 2026-09-04 through 2026-09-11 horizon.
- D083–D088 were independently researched, reviewed, validated, and atomically published. Both permitted Henry model attempts failed during fact-chunk generation for each, so verified complete public-domain commentary links were retained without admitting a fabricated layer.
- Every publication passed exact payload and manifest readback, exact plan/manifest-prefix equality, unchanged narrow sharing, and authenticated bootstrap health. The repeated final audit verified all eight horizon readings and the 35-entry prepared prefix.
- The one permitted historical protocol refresh updated D073/Zechariah 4 to `daily-study-protocol/v1`, added a directly consulted Jewish interpretive witness and a genuine editorial clarification, preserved its approved checksum-bound Henry layer and prior versions, and passed manifest-last Drive publication plus authenticated bootstrap.
- The deterministic follow-up protocol queue now names D072; the one-refresh limit prevents processing it in this run, and the Henry-only queue is not eligible until the protocol lane returns `none`.

## Milestones

1. Research, draft, directly review, validate, and publish D083 with the verified complete-commentary fallback.
2. After exact readback and bootstrap health, reevaluate and repeat for each earliest remaining gap through 2026-09-11.
3. Verify the complete local and Drive horizon, prepared-prefix plan, manifest bindings, and authenticated bootstrap.
4. Run at most one protocol-refresh order, with the same direct-review and manifest-last rules.
5. Run repository-wide checks, commit/push safe tracked metadata, publish code-only Pages if changed, and update durable project and automation state.

## Acceptance criteria

- Every reading through T+7 has current protocol metadata, exact content hash, adequate source coverage, one historical-context assessment, and either reviewed Henry shards or a verified full-commentary link.
- Every publication records `rollingPlanBytesExact`, `rollingPlanManifestPrefixExact`, and `authenticatedBootstrapSucceeded` as true.
- The final `study:next` result is `none` or `plan_complete`, followed by repeated horizon/plan/bootstrap verification.
- `npm run safety`, private/source/schema validation, tests, build, Pages verification, and `npm run check` pass without weakening a gate.

## Progress

- [x] Read automation memory, repository instructions, required workflow documents, linked project documents, and the full commentary skill.
- [x] Fetch and verify `origin/main`; create and verify the clean isolated worktree with checked private-store symlinks.
- [x] Validate the D083 rolling work order.
- [x] Run the strict Henry controller and record the Spark/Luna-low model-execution failures and verified-link requirement.
- [x] Complete D083 primary research, content, review, validation, and publication.
- [x] Drain all remaining bounded gaps and verify the horizon.
- [x] Complete one protocol refresh with exact publication and bootstrap verification.
- [x] Complete tracked release bookkeeping, safe commit/push, and code-only Pages publication. The generated token Apps Script backend was byte-identical, so no deployment update was required.

## Exact next action

On the next scheduled run, reverify the rolling T+7 horizon first; if it remains complete, the deterministic protocol-refresh lane resumes with D072. Do not run Henry-only backfill until the protocol lane returns `none`.
