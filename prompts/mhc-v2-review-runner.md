# Installed Henry v2 review task

Run the supplied verified absolute Node executable and installed launcher with
reviewer work-order. Read its exact instructionsPath and selected private bundle
or editorial packet. Follow the returned v2 review/repair/publication contract and
commands. A legacy-review-recovery result uses its supplied retained v1 review
instructions; it does not authorize legacy generation or selecting a new reading.

Review every sentence against its supporting evidence, source scope, material
identities and relationships, and meaningful qualifications. Preserve independent
review; never infer approval from mechanical validation. Write only the returned
review or repair path and run the exact returned applyCommand or repairCommand.

For publish_required or recover_publish, use the existing authorized private
one-reading publisher and exact readback/live-health procedure. The existing
manager-pinned start-cortan-daily-workspace.ps1 publication bootstrap is permitted
for these actions: its verified offline same-commit clone, locked offline npm ci,
and four canonical private-store junctions are the established publishing path.
Do not create
resources, change sharing, broaden the preparation horizon, or rewrite the main
study. Record only a genuine exact publication receipt, then reconcile work-order
again. Never mark the reading complete from a local attachment. Source changes,
permission blocks, or mismatched approvals stop their dependent action.

For review, do not fetch Git, create an ad hoc checkout, install dependencies, or
alter the installed runtime. The publication-bootstrap exception above does not
authorize any other environment change. Keep the current exact reviewer model
and reasoning setting. If the host
requires elevation, request only the returned installed local command; a denial
is a controller block, never generation/model failure. Final output contains only
the reading, stage, real outcome and blocker, without private content or IDs.
