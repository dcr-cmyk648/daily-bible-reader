# Henry Windows release and activation handoff

Prepared in the isolated repair worktree; no production installation or task
activation has occurred. Keep the existing Henry generation, review, and
supervisor automations paused. The daily devotional manager owns its automation
and continues independently with verified complete-commentary links.

## Concrete release sequence

1. Review the isolated source diff and validation evidence in the execution plan.
   Obtain explicit approval to commit and push this code-only repair. The five
   Windows migration source/test adaptations in this diff match the canonical
   checkout's existing portability work; preserve its six uncommitted migration
   files when integrating. Do not replace the canonical checkout wholesale.
2. Commit the accepted repair on an approved branch and run the existing remote
   code checks. This runner changes no frontend/backend, authentication, storage,
   ESV behavior, public assets, or deployment pointer. An app redeployment or new
   iPhone gate is unnecessary for this offline-only change.
3. From a clean checkout of that exact accepted commit, install using the existing
   canonical private roots. The installer intentionally refuses uncommitted
   runtime source. Bind the existing Spark and Luna automation IDs locally from
   their saved definitions; do not place private task/configuration state in Git.

   ```powershell
   & 'C:\Program Files\nodejs\node.exe' scripts/install-mhc-native-runtime.mjs `
     --project-root 'C:\Users\rowla\Projects\BibleApp' `
     --revision <accepted-40-character-commit> `
     --spark-automation-id <existing-spark-task-id> `
     --luna-automation-id <existing-luna-task-id>
   ```

   The result reports the absolute Node executable, launcher path, source commit,
   and manifest digest. Releases contain only committed scripts, schemas,
   prompts, and factual plan/config fixtures. Configurations and checkpoints are
   private and are not replacement commentary stores. Installation performs no
   model invocation, source download, Git fetch, or private publication.
4. Before any generation activation, separately inspect canonical historical
   work-item/ledger paths and provenance. Mac absolute paths and live leases need
   an explicit operational decision by the release owner; do not rewrite,
   reconcile, adopt, or reset them automatically. Installation checks runner
   checkpoints, but does not certify old native work-item leases as safe to use.
   Do not run a generation command until this gate is resolved. Retained reviewed
   library objects and verified source links must remain unchanged.
5. Prepare changes to the existing Henry tasks only. Bind their saved project to
   the canonical checkout and use the installed generation/review prompt bytes.
   Keep exact Spark medium primary and exact eligible Luna low fallback; no
   nested Codex, alternate model, API key, or new private store. Preserve four
   primary slots; the proposed fallback offset is two hours and review is hourly.
   These are drafts requiring activation approval, not schedules installed by
   this repair. Do not alter daily devotional automation.

## Installed Windows task contract

The installed entry is:

```powershell
& 'C:\Program Files\nodejs\node.exe' 'C:\Users\rowla\Projects\BibleApp\private-content\automation\mhc-runtime\launcher.mjs' spark
```

Use `luna` for the existing exact Luna-low task and `reviewer work-order` for the
independent reviewer. Author only the returned bounded candidate and call the
returned `advanceCommand`; its PowerShell quoting handles spaces/apostrophes.
Read the returned instructions and validation file completely. Continue through
consecutive candidates until a review handoff, budget checkpoint, deterministic
block, or safe no-op. Never infer a missing candidate is empty content.

On Cortan the sandbox denied Node child processes with `spawn EPERM`; the
fabricated tests ran through explicitly approved elevated tool execution. Saved
worker instructions must request the exact installed local command with
`sandbox_permissions: require_escalated` when that host boundary applies. Do not
misclassify a denied process launch as model failure, fetch/DNS trouble, or Luna
eligibility. Do not change global hooks, trust, policy, or security settings.
Per-wake GitHub/bootstrap access has been removed; host execution authorization
still must be exercised correctly.

A wake retains its 60-minute/48-submit budget through repeat starts in the same
scheduled slot. New leases stop at 45 minutes. Resume only via the returned
checkpoint; immutable artifacts and exact ledger admission remain authoritative.
Terminal or completed checkpoint history stays preserved across runtime upgrades;
active wakes refuse upgrades. A plan/automation-identity change requires another
accepted committed runtime installation. The installed factual plan is pinned to
that source revision, so a later prepared-prefix extension must be included in
an authorized upgrade before Henry can select its new readings.

## Operational acceptance still required

After source release and explicit activation approval, use a naturally authorized
reading, not newly generated test prose. Record one complete installed exact-model
run through actual independent review discovery, followed by a naturally
scheduled wake without a manual follow-up. If no such reading is authorized,
leave generation paused. Fabricated controller tests are not model throughput or
unattended scheduler proof. Independent editorial approval, hash-bound review,
durable-library finalization, attachment, manifest-last private publication, and
exact live readback remain the existing publication lane's responsibility.

## Coordinated activation preflight — 2026-09-12

The manager relayed the user’s explicit instruction to activate the repair. The
manager owns commit/push authorization, installation, task restoration, and
activation. This repair task performed only read-only canonical preflight; no
lease, content, task, or installed runtime was changed.

All six canonical storage roots are real directories, with no junction/symlink
redirection in the native work store. All 221 files in that store were hashed
before and after inspection and remained identical. There are 24 retained work
items and five ledgers, with zero active or authenticated historical leases.
No lease adoption or ledger reconciliation is necessary for the immediate work.

The existing selector names D087 next, among 14 eligible published fallbacks.
Its nine validated events contain eight winning chunks: Psalm 148 has four
Luna chunks, Psalm 149 has three Spark chunks, and Psalm 150 has one Luna chunk.
The extra validated Spark event for Psalm 148 is historical and does not replace
that chapter’s winning Luna provenance. All nine retained candidates still
validate, and their reconstructed facts/output exactly match their immutable
stages. Only Psalm 150:5–6 remains; after coordinated activation it requires a
new Luna lease under the already recorded eligible transfer.

D084’s actual read-only reviewer work order returns completed/manifest_backed.
Its retained old raw candidate formats must not be regenerated or treated as new
review debt. D082, D083, and D086 have terminal historical attempts, not active
leases; any later selected work requires a fresh controller-admitted lease. Four
old instruction files contain Mac path text, all for validated or superseded
work. The missing D087 final chunk has no old packet and will receive a fresh
Windows-bound packet. Preserve the old instruction files as evidence.

No Henry runtime exists in the canonical project. The current Cortan automation
directory has no Henry definitions; migration backups preserve their original
IDs and model/reasoning bindings. The manager has those exact bindings in the
private coordination handoff. Restore those identities instead of inventing
new controller IDs, which would invalidate D087’s current work-item definitions.
Remaining gates are coordinated source release/installation, task restoration
with correct Windows execution authorization, and real scheduled operational
proof. Daily devotional service remains untouched.

### Manager handoff update

The manager has now restored the three Henry task definitions on Cortan under
the original stable IDs. A read-only check confirms all are PAUSED with exact
Spark medium, Luna low, and Terra high bindings. The source release remains
uncommitted; the manager is obtaining the explicit commit/push approval required
by AGENTS.md. Activation itself is approved. No task was enabled by this repair
task, and no runtime was installed.

The installed reviewer command surface stops at work-order, prepare, apply, and
finalize. It does not expose attachment, private content validation/bundling,
live-health, or Drive publication. The existing attachment/validation scripts
also resolve schema/plan inputs from the working checkout, and Drive publication
uses the established connector lane. Therefore the reviewer task must explicitly
handoff attachment/publication to the manager’s separately verified existing
lane, or receive a separately reviewed command-surface extension before it can
claim fully installed end-to-end publication. This is an integration limitation,
not a current private-data or historical-lease blocker. The manager was notified
before activation. All 22 code fingerprints remain unchanged.

### Publication lane resolved by manager

The manager explicitly routed the PAUSED reviewer from installed native review
commands into the existing separately verified offline Cortan publication
workspace. The manager reports that its pinned baseline plus checksum-bound
Windows files passed all 342 baseline/private/build/Pages gates. The reviewer
will use that workspace solely for the selected reading’s attachment, validation,
content-first/manifest-last Drive publication, and exact-reading live health.
It may not invoke daily preparation or protocol refresh. This resolves the
reviewer integration gate without expanding the native launcher or changing any
of the 22 validated repair code files. Commit/push approval and coordinated
installation/activation remain with the manager.
