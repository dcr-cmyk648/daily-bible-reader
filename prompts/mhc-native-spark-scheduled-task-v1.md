# Native Henry Spark lane

Run at 05:15, 11:15, 17:15, and 23:15 America/Detroit using exact `gpt-5.3-codex-spark` at medium reasoning. Fetch `origin/main`, create an isolated worktree, and verify/link the existing ignored private stores; never substitute empty stores. Run exactly `npm run mhc:native:prepare -- --model gpt-5.3-codex-spark --automation-id <this-automation-id>`. Prepare durably binds this Spark/Luna slot to one reading and exact source/chunk state before it creates a model work item. If it reports `state=deterministic_block`, stop successfully with that safe code; the paired Luna run must consume the same block and may not select another reading. If its safe JSON report includes `workItemPath` for a prepared or already-active lease, use that repository-relative path exactly for controller commands and use the reported canonical `candidatePath` only for authoring. Read only that directory's bounded `source-view.json`, `instructions.txt`, complete `mhc-native-candidate.schema.json`, conspicuously fabricated `candidate.example.json`, and `prompts/mhc-native-spark-worker-v1.md`; actually generate `candidate.json` at `candidatePath` before the first submit. Read that exact path back and verify every requested verse has one complete fact brief and verse draft. If a write fails, preserve and retry the same complete draft at `candidatePath`; never replace it with or submit an empty shell, placeholder, partial candidate, or guess. A candidate is incomplete—not generated—if it has zero fact briefs, zero verse drafts, or lacks either record for any requested verse. Run `npm run mhc:native:submit -- --work-item <workItemPath>` only after successful readback. After every failed submit, you MUST read only the exact reported `validationPath`, repair `candidate.json` with materially different bytes, reread it, and resubmit against the same lease. A persistence failure is not a submit attempt: do not submit or terminally fail it; safe-report `stage=persistence/code=NATIVE_CANDIDATE_SAVE_FAILED`. Make at most two repairs (three submit attempts total), and use both remaining repairs after the first failure unless an earlier repair validates. The controller rejects unchanged candidate bytes and refuses early terminal failure; unresolved terminal failure requires all three distinct failed submits. Only then may Spark run `npm run mhc:native:fail -- --work-item <workItemPath> --code NATIVE_CANDIDATE_UNRESOLVED`; do not record historical cooldown, because the terminal Spark failure is eligible for Luna. Never report success after a failed submit. Run `npm run mhc:native:assemble -- --work-item <workItemPath>` only after a successful submit. `action=none/state=reading_incomplete` is normal multi-chunk progress: exit successfully and wait for later chunks; it never creates a review handoff. Handle one chunk/reading boundary only. Do not review, apply, sync, modify a manifest, publish, push, or expose private prose, source atoms, IDs, or secrets; do not repeat absolute private paths in the final run summary. Safe-report normalized lane/state/code and that fallback/manifest remain unchanged on failure.

A controller/bootstrap/host-policy stop is not a Spark model or candidate-admission failure. In particular, `NATIVE_AUTOMATION_BLOCKED_BY_HOST_POLICY` must be reported exactly as `stage=controller/code=NATIVE_AUTOMATION_BLOCKED_BY_HOST_POLICY` with the prior fallback and manifest unchanged; do not submit, run `mhc:native:fail`, append a Luna-eligibility event, or claim Luna failed or was unavailable. One authenticated terminal `NATIVE_CANDIDATE_UNRESOLVED` event after the required three distinct failed submits is the only candidate-admission handoff signal and remains idempotent for that lease.
