# Prepare the 2026-09-10 T+7 horizon

## Goal

Publish the exact current-through-T+7 private study prefix for Detroit date 2026-09-10, crossing from the Celebration bridge into the activated long-term calendar through Genesis 1 on 2026-09-17, then perform the one permitted prior-reading protocol refresh and release the validated code-only support changes.

## Requirements

- Preserve stable reading IDs, comments, highlights, reader identity, ESV boundaries, narrow Drive sharing, and immutable rollback deployments.
- Prepare and publish only the earliest exact missing/stale horizon reading at each step, never beyond T+7.
- Research, write, review, validate, and publish Genesis 1 under `daily-study-protocol/v1`, with a verified complete Matthew Henry fallback link and distinct concise/expanded historical-context layers.
- Publish private payloads content-first and manifest-last, with exact readback and live-health verification.
- After the horizon is ready, refresh at most one deterministic prior-reading protocol work order.
- Validate and publish the code-only Pages/Apps Script changes without moving rollback deployments.

## Constraints and non-goals

- No Scripture wording, private Drive IDs, reader credentials, user emails, or commentary content in Git or Pages.
- No runtime AI, new Google/ESV resources, access-model changes, sharing changes, or long-term generation beyond the exact T+7 edge.
- The canonical checkout has unrelated user changes; all tracked work occurs in the isolated worktree.
- The primary task owns commentary research, synthesis, editorial review, and publication. The bounded worker owns only the backend source-boundary regression fix.

## Relevant repository state

- Isolated base: `a5a6a9a9b513d81f8c4ab5bd24a417b459bfc832` from `origin/main`.
- D091, D092, `LTP-0001-GEN-INTRO`, and `LTP-0002-GEN-001` have been published and verified; the private prefix contains 41 readings through 2026-09-17.
- The fresh rolling work order is `none` / `horizon_ready`, and authenticated live health reports all eight current-through-T+7 readings ready.
- Genesis 1 is a 3,028-word current-protocol synthesis using ten directly checked sources; its verified complete Henry chapter link remains an explicit fallback debt.
- The single permitted historical refresh updated D071/Zechariah 3 to the current protocol with a 2,078-word study, eight sources, and its approved Henry layer preserved exactly. D070 is next but is out of scope for this run.
- Token backend version 32 contains the reviewed source-boundary fix; both rollback deployments remain version 23 and Apps Script HEAD has been restored exactly.

## Decisions

- Treat schedule-entry `sourceIds` as factual calendar provenance, not commentary-source dependencies. Commentary validation collects citations and the explicit Henry fallback source only.
- Reuse the existing Genesis pilot only as editorial background; publish a new current-protocol synthesis, metadata payload, and additive registry revision.
- Keep Genesis 1 interpretation calibrated: confess core creation claims clearly, present the main days readings fairly, and label scientific/chronological harmonizations according to evidence.

## Milestones

1. Finalize and validate the Genesis 1 editorial package and additive source registry.
2. Extend the private prefix, upload versioned payloads, publish the manifest last, and verify exact live health.
3. Complete at most one deterministic protocol backfill, update durable state, and run the final release gate.
4. Publish the authorized code-only artifacts, verify live bytes/health, and commit/push the isolated tracked change set if the automation authorization permits.

## Acceptance criteria

- The private prefix has exactly 41 ordered readings through Genesis 1 and no later entry.
- Genesis 1 passes schema, citation, source, historical-depth, rights, Scripture-exclusion, hash, and private-bundle checks.
- Remote content, metadata, config, plan, registry, and manifest match local candidates exactly; permissions remain narrow and the live exact-reading health is ready.
- The one permitted prior-reading refresh either publishes successfully or stops safely at the first actionable failure.
- Focused tests, `npm run check`, private bundle validation, repository safety, immutable backend comparison, and Pages release checks pass.

## Progress

- [x] Publish and verify D091, D092, and the Genesis introduction.
- [x] Diagnose, regression-test, and deploy the commentary-source boundary fix.
- [x] Finalize and publish Genesis 1.
- [x] Run one prior-reading protocol refresh.
- [x] Complete local release validation and durable-state updates.
- [ ] Commit, push, and verify the authorized remote Pages publication.

## Validation evidence

- Backend/front-end focused tests and the final 342-test tracked suite pass.
- Token backend version 32 and restored accessing-user HEAD were downloaded and byte-verified; rollback deployments remain version 23.
- Genesis 1 and D071 content, metadata, config/plan/registry where applicable, and manifest were published content-first and manifest-last with exact readback and unchanged narrow sharing.
- Local validation reports 38 schemas, 41 private-prefix readings, 39 bridge entries, 315 registered sources, and no stored Scripture.
- Authenticated exact-reading health reports Genesis 1 and D071 ready; the full eight-reading horizon reports 8/8 ready with seven chapter-level Henry fallbacks and no unavailable layers.
- Final release validation passes repository safety over 394 files, all validators, 342/342 tests, every build, exact Pages verification, and the 85-file private bundle.
- A fabricated 390×844 browser smoke shows Genesis introduction and Genesis 1 prepared, Genesis 2 locked, no horizontal overflow, and no browser warnings or errors.

## Exact next action

Commit and push only the reviewed safe tracked change set, wait for the required GitHub workflows, then verify the live Pages bytes and MIME types exactly.
