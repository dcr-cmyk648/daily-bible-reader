# Daily devotional service authority and visible status

User authorization, September 24, 2026: restore and maintain the actual Detroit
current-through-T+7 preparation window and reduce repeated approval requests.
Routine reviewed devotional publication and necessary bounded BibleApp maintenance
are authorized, including isolated repairs, tests, safe commits/pushes to the
existing repository, validated releases to the existing Pages/token service, and
installation of accepted daily/Henry runtime corrections. Do not ask again for
these same routine actions. This supersedes the earlier daily-service blanket ban
on code delivery; it does not override tool enforcement or release validation.

Preserve source review, atomic publication, private content boundaries, sharing,
identities, the immutable version-23 rollback, and required installed-phone gates.
New destinations, new/broader access, purchases, destructive actions, plan redesign,
or work beyond T+7 still need their applicable authorization. Do not change global
Codex permission policy or disable automatic review. A real denial remains a block.

The app computes missing publication directly from the authenticated prepared ID
prefix on every home render, independent of commentary/ESV download success and
selected calendar date. Its alert sits directly below the calendar. Full payload checks
continue to detect missing components; manifest membership is not a final health
certificate. Ordinary gaps are amber; missing today/tomorrow and service failures
are red. Dustin alone receives private operational approval details.

At a new failure or required user decision, prepare a private status candidate:

`node scripts/prepare-service-status.mjs approval_required "What is blocked" "The exact approval needed"`

Other states are `running`, `blocked`, and `ready`. Use short safe prose without
credentials, private IDs, commentary or raw error dumps. The helper only stages
`private-content/automation/staging/service-status/manifest-candidate.json`.
Fetch and compare the existing live manifest immediately before updating it via
the connected Drive tool; preserve every payload pointer and concurrent publication,
then verify exact bytes and authenticated bootstrap status. Never blindly upload
a stale candidate. No new resource, sharing change, or app deployment is needed.
Use `approval_required` only for a specific unresolved decision outside standing
authority, not for routine permitted actions or technical failures. These notices
stay red with their recorded timestamp until explicitly updated; they do not expire
silently. Tell the user to open BibleApp Manager in Codex; the reader cannot grant
Codex tool approvals. If status publication itself is blocked, report directly in
the task and do not claim the in-app notice was delivered.

After approval, publish `running`; after the exact full horizon health gate passes,
publish `ready`. Preserve this field in every reading manifest promotion and verify
the resolved state through live bootstrap. A shortfall remains visible even when
an operational receipt says ready. Do not suppress a shrinking buffer: report new
failures, unresolved required action and worsening coverage, especially fewer than
four future prepared days. Healthy unchanged maintenance remains quiet.
