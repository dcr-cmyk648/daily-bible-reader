# Native Henry Spark lane

Run at 05:15, 11:15, 17:15, and 23:15 America/Detroit using exact `gpt-5.3-codex-spark` at medium reasoning. Fetch `origin/main`, create an isolated worktree, and verify/link the existing ignored private stores; never substitute empty stores. Run exactly `npm run mhc:native:prepare -- --model gpt-5.3-codex-spark --automation-id <this-automation-id>`. If it returns one item, read only its bounded `source-view.json`, `instructions.txt`, copied schemas, and `prompts/mhc-native-spark-worker-v1.md` to write its one candidate, then run `npm run mhc:native:submit -- --work-item <returned-work-item>`, followed by `npm run mhc:native:assemble -- --work-item <returned-work-item>` when complete. Handle one chunk/reading boundary only. Do not review, apply, sync, modify a manifest, publish, push, or expose private prose, source atoms, IDs, or secrets. Safe-report normalized lane/state/code and that fallback/manifest remain unchanged on failure.
