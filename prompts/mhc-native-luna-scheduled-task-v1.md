# Native Henry Luna fallback lane

Run at 05:25, 11:25, 17:25, and 23:25 America/Detroit using exact `gpt-5.6-luna` at low reasoning. Fetch `origin/main`, create an isolated worktree, and verify/link existing ignored private stores. Run exactly `npm run mhc:native:prepare -- --model gpt-5.6-luna --automation-id <this-automation-id> --primary-automation-id <spark-automation-id>`. Prepare consumes the durable pair selected for that primary Spark slot; it never reruns the cooldown selector for a different reading. Continue only when that same selected reading has an eligible Spark model failure or authenticated missed/stale primary and the safe report includes `workItemPath`. If it reports `state=deterministic_block`, stop successfully and retain that same reading/block; do not record cooldown and do not reinterpret it as a missed Spark run.

If Spark safe-reported a controller/bootstrap/host-policy stop such as `NATIVE_AUTOMATION_BLOCKED_BY_HOST_POLICY`, that is not a Luna eligibility signal. When prepare has no authenticated eligible transfer, stop without generation and report `stage=controller/code=NO_ELIGIBLE_SPARK_TRANSFER`; retain the prior fallback and manifest and do not claim that Luna failed or was unavailable.

The assigned exact Luna-low agent is itself the only generation worker for this one prepared work item. The safe prepare report supplies both `workItemPath` for controller commands and an absolute canonical `candidatePath` inside the verified real private work-item directory for authoring. Read only that directory's bounded `source-view.json`, `instructions.txt`, complete `mhc-native-candidate.schema.json`, and conspicuously fabricated `candidate.example.json`, plus `prompts/mhc-native-luna-worker-v1.md`. Use your existing repository file-editing capability to actually generate `candidate.json` at the exact reported `candidatePath` before the first submit. Read that exact path back and verify every requested verse has one complete fact brief and one complete verse draft. If the write fails, preserve the complete substantive draft in your current context and retry the same bytes at `candidatePath`; never replace it with or submit an empty shell, placeholder, partial candidate, or guessed reconstruction. A candidate is incomplete—not generated—if it has zero fact briefs, zero verse drafts, or lacks either record for any requested verse. Do not run `codex exec` or any other command that launches a nested Codex/model process. Do not spawn, delegate to, or hand the work item to another child agent. Never substitute Terra, Sol, Spark, or any other model for the assigned exact `gpt-5.6-luna` low-reasoning worker.

Run `npm run mhc:native:submit -- --work-item <workItemPath>` for that same one work item only after successful candidate readback. After every failed submit, you MUST read only the exact `validationPath` from prepare, repair `candidate.json` directly with materially different bytes, reread the complete saved file, and resubmit against the same lease. A candidate persistence failure is not a model-validation attempt: do not submit, do not run `mhc:native:fail`, and report only `stage=persistence/code=NATIVE_CANDIDATE_SAVE_FAILED` with the fallback and manifest unchanged. A first failed submit may not proceed to terminal failure: use both remaining repair attempts unless an earlier repair validates. Make at most two repairs (three submit attempts total); when no attempt validates, all three submits must be distinct before unresolved terminal failure. The controller rejects unchanged candidate bytes and refuses terminal failure before three distinct failed submits. Only after all three distinct attempts fail may you run `npm run mhc:native:fail -- --work-item <workItemPath> --code NATIVE_CANDIDATE_UNRESOLVED`, then record the safe historical cooldown attempt with `npm run mhc:backfill:record -- --reading <safe-readingId> --outcome model_failure --stage validation --code NATIVE_CANDIDATE_UNRESOLVED`; retain the fallback and manifest. Never report success after a failed submit. Run `npm run mhc:native:assemble -- --work-item <workItemPath>` only after a successful submit. `action=none/state=reading_incomplete` is normal multi-chunk progress: exit successfully and wait for later chunks; it never creates a review handoff. The controller alone decides failure/staleness and Luna restarts the chapter at chunk 1. Never create a fallback, manifest, publication, review, sync, push, or private disclosure. Do not repeat absolute private paths in the final run summary. Safe-report only normalized state/code and unchanged fallback/manifest; never expose private prose, source atoms, IDs, or secrets.
