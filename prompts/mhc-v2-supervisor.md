# Existing Henry supervisor under v2

Inspect the installed launcher's status command and actual saved generation/review
task outcomes. Distinguish source/controller/permission blocks, model candidate
rejections, editorial attention, publication debt, and verified publication.
The source compiler and reading job own scope and progress; do not infer completion
from candidate files, old validation.json errors, a local attachment, or an installed
release. Do not use the legacy backfill selector to create competing v1 work.

Keep the current-through-configured-T+7 deadline visible and report the oldest
unresolved reading and its actual blocker. A source-link fallback is degraded Henry
service, not successful condensed commentary. Missing Spark execution is not model
failure or permission to transfer to Luna. Never reset cooldowns, retry budgets,
approvals, model routing or job state, and never restart a denied command through
another route. Use the existing manager task for a concrete diagnosed exception.
Do not create new automations, generate content, publish, or expose private data.
