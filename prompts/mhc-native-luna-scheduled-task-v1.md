# Native Henry Luna fallback lane

Run at 05:25, 11:25, 17:25, and 23:25 America/Detroit using exact `gpt-5.6-luna` at low reasoning. Fetch `origin/main`, create an isolated worktree, and verify/link existing ignored private stores. Run exactly `npm run mhc:native:prepare -- --model gpt-5.6-luna --automation-id <this-automation-id> --primary-automation-id <spark-automation-id>`. Prepare consumes the durable pair selected for that primary Spark slot; it never reruns the cooldown selector for a different reading. Continue only when that same selected reading has an eligible Spark model failure or authenticated missed/stale primary and the safe report includes `workItemPath`. If it reports `state=deterministic_block`, stop successfully and retain that same reading/block; do not record cooldown and do not reinterpret it as a missed Spark run. Read only that directory's bounded `source-view.json`, `instructions.txt`, copied schemas, and `prompts/mhc-native-luna-worker-v1.md`; actually generate `candidate.json` before the first submit. Run `npm run mhc:native:submit -- --work-item <workItemPath>`. If submit fails, read only that work item's `validation.json`, repair `candidate.json`, and resubmit against the same lease. Make at most two repairs (three submit attempts total). If Luna still cannot validate, first run `npm run mhc:native:fail -- --work-item <workItemPath> --code NATIVE_CANDIDATE_UNRESOLVED`, then record the safe historical cooldown attempt with `npm run mhc:backfill:record -- --reading <safe-readingId> --outcome model_failure --stage validation --code NATIVE_CANDIDATE_UNRESOLVED`; retain the fallback and manifest. Never report success after a failed submit. Run `npm run mhc:native:assemble -- --work-item <workItemPath>` only after a successful submit. `action=none/state=reading_incomplete` is normal multi-chunk progress: exit successfully and wait for later chunks; it never creates a review handoff. The controller alone decides failure/staleness and Luna restarts the chapter at chunk 1. Never create a fallback, manifest, publication, review, sync, push, or private disclosure. Safe-report only normalized state/code and unchanged fallback/manifest; never expose private prose, source atoms, IDs, or secrets.
